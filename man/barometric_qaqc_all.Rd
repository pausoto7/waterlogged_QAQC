% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baro_qaqc_all.R
\name{barometric_qaqc_all}
\alias{barometric_qaqc_all}
\title{Run barometric QA/QC for all baro stations in metadata}
\usage{
barometric_qaqc_all(
  baro_data_path,
  metadata_path,
  path_to_output_folder,
  log_root,
  temp_low_limit = -40,
  temp_high_limit = 50,
  pressure_low_kpa = 85,
  pressure_high_kpa = 105,
  spike_threshold_kpa = 1.5,
  flatline_n = 6,
  user = Sys.info()[["user"]]
)
}
\arguments{
\item{baro_data_path}{Root directory containing processed barometric CSV
files. All subdirectories are searched recursively for files matching
\code{"<station>_BARO_*.csv"}, where \verb{<station>} comes from the metadata.
For example: \code{"data/testing/processed"}.}

\item{metadata_path}{Path to the metadata CSV used by \code{\link[=QAQC_metadata]{QAQC_metadata()}},
e.g. \code{"data/testing/raw/testing_metadata.csv"}. Must include
\code{site_station_code}, \code{metric}, and barometric station definitions
(\code{metric == "barometric"}).}

\item{path_to_output_folder}{Root directory where yearly QA/QC barometric
files will be written. Year- and \code{processed}-subfolders are created as
needed, e.g. \code{"data/testing/processed"}.}

\item{log_root}{Root folder where QA/QC logs are written by
\code{\link[=barometric_qaqc]{barometric_qaqc()}} and related functions, typically the same as
\code{path_to_output_folder}, e.g. \code{"data/testing/processed"}.}

\item{temp_low_limit}{Lower bound (\u00B0C) for plausible barometric logger
temperatures. Values outside this range may be flagged or removed.}

\item{temp_high_limit}{Upper bound (\u00B0C) for plausible barometric logger
temperatures.}

\item{pressure_low_kpa}{Lower bound (kPa) for plausible barometric
pressure.}

\item{pressure_high_kpa}{Upper bound (kPa) for plausible barometric
pressure.}

\item{spike_threshold_kpa}{Threshold (kPa) for flagging sudden pressure
spikes between consecutive timesteps.}

\item{flatline_n}{Integer; number of consecutive identical values used to
flag potential flatlining in the pressure series.}

\item{user}{Character username written into the QA/QC log. Defaults to
\code{Sys.info()[["user"]]}.}
}
\value{
A single long-format data frame created by binding the QA/QC'd
barometric data for all stations. If no stations are processed
successfully, an empty tibble is returned and a warning is issued.
}
\description{
Reads all barometric logger files for stations listed in the metadata
(where \code{metric == "barometric"}), applies \code{barometric_qaqc()} to each
station, writes yearly QA/QC CSVs (v0.1), and returns a combined data
frame of all QA/QC'd barometric data.
}
\details{
Files are expected to follow the naming pattern
\code{"<site_station_code>_BARO_YYYYMMDD_YYYYMMDD_v*.csv"} and live somewhere
under \code{baro_data_path}.
}
\seealso{
\code{\link[=barometric_qaqc]{barometric_qaqc()}}, \code{\link[=QAQC_metadata]{QAQC_metadata()}}, \code{\link[=add_nearest_baro]{add_nearest_baro()}}
}
